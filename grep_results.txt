./Tests/Performance/SearchPerformanceTests.swift:                content = "讨论系统优化策略，包括架构层面和代码层面的改进方案。"
./Docs/Design/DETAILED_DESIGN.md:| 优先级 | 问题 | 目标文件 | 方案 |
./Docs/Requirements/LOCALIZATION.md:新增语言需在 `Localized.swift` 中注册对应的 Locale，确保日期格式与搜索分词器同步切换。`ThemeManager` 仅负责暗/亮色彩方案与主题色管理，不参与语言环境切换。
./Docs/guides/implementation-patterns.md:## Swift 6 编译器变通方案
./Docs/guides/implementation-patterns.md:- **`static let` 配合自定义 `Color(light:dark:)` 初始化器会失败**：Swift 6 在使用带嵌套闭包的自定义初始化器的 `static let` 属性时可能产生 "failed to produce diagnostic for expression" 错误。**变通方案**：改用 `static var` 计算属性：
./Docs/superpowers/specs/2026-05-08-feature-architecture-refactor-design.md:## 3. 实施计划 (方案一：原子结构先行)
./Sources/Shared/Core/Utilities/ThemeManager.swift:// 1. 智适应色彩方案：支持深色（Dark）、浅色（Light）及跟随系统的主题模式切换，并内置了从旧版 isDarkMode 标志位的全量迁移逻辑。
./Sources/Shared/Data/Persistence/DatabaseManager.swift:            // 回退方案：如果将来增加了其他 DatabaseWriter 实现
./Sources/Shared/Domain/Logic/LLMModels.swift:        // 兜底方案：如果 JSON 未能加载（如尚未打包），使用硬编码数据
./Sources/Shared/Domain/Processors/Graph/GraphLayoutProcessor.swift:// 功能说明: 本文件实现了知识图谱的力导向布局处理器（GraphLayoutProcessor），通过高度优化的物理仿真算法为复杂的知识关联网络提供自适应的空间排布方案。
./CLAUDE.md:| [implementation-patterns.md](Docs/guides/implementation-patterns.md) | Swift 6 变通方案、图谱模式、合成文档、缓存策略、Mermaid、UI 框架等 |
